# 版本基线与当前状态

本页回答两个问题：这本书依据哪一版代码写成，以及哪些能力已经有代码、哪些还只是后续方向。

## 1. 本次核对基线

核对日期：2026-08-16。

| 仓库 | 本书核对的提交 | 当时所在分支 | 与 OLL 的关系 |
| --- | --- | --- | --- |
| octos-lesson-language | `991a247` | `codex/interactive-learning-tasks` | DSL `0.1`，npm 包 `0.1.0-rc.1` |
| learning-coach | `bb79473` | `codex/shared-variable-generation` | 直接锁定 OLL `991a247` |
| octos-web | `96ae5e3` | `codex/update-oll-function-plot-runtime` | 直接锁定 OLL `991a247` |
| Octos | `3134173f` 加本地提交 `fc2ef7e6` | `fix/plugin-session-workspace` | `main` 已有 Skill Action；本地提交把会话工作区安全地传给 Skill |

这是一组可以互相配合的开发版本，不表示四个仓库都已合并到各自 `main`。尤其不要只看分支名判断兼容性：真正决定 learning-coach 和 octos-web 使用哪一版 Runtime 的，是它们 `package.json` 中锁定的 OLL commit。

核对当前 checkout：

```bash
git status --short --branch
git rev-parse HEAD
git log -1 --oneline
```

核对消费方锁定的 OLL：

```bash
node -p "require('./package.json').dependencies['octos-lesson-language']"
```

## 2. 当前已经实现的能力

以下能力在上述基线中有代码和自动测试：

- Authoring Schema、语义校验、Normalizer、Reducer 和 Player；
- text、math、shape、diagram、geometry、plot、scene3d、image、table、note；
- 共享数值变量、Geometry/Plot/Scene3D Binding、自动动画；
- 滑杆、圆周角度控制点、3D 旋转/缩放/预设/复位；
- 可判定的变量目标任务和 3D 视角任务；
- 盒体、球、圆柱、圆锥、`z=f(x,y)` 曲面，以及轴对齐截面与目标对象的真实交线或截面；
- 相连主视觉的邻近布局、讲解辅助节点时保留相关主视觉、避开宿主浮层的相机取景；
- 学生笔迹、矩形框选、自由套索、只读选区快照和刷新恢复；
- 课程重播时暂时隐藏旧笔迹，重播结束后恢复并合并重播期间的新笔迹；
- 选区图片、稳定白板目标和原稿校验和共同驱动“问小章鱼”；
- 局部解释卡片可最小化为选区附近的 `?` 按钮；
- 分段生成、合法前缀提前交付，以及单个视觉组件失败时局部降级；
- 学生拖动变量、几何点、3D 视角或白板画面时不打断 TTS；教师演示某个变量时才临时取得该变量的控制权。

## 3. 生成系统现在怎样对待模型的不确定性

模型输出不是业务事实。当前实现采用下面的边界：

1. 模型提出课程要求、视觉对象和教学步骤的候选结果；
2. 程序检查结构、引用、表达式、变量、可操作控件和任务条件；
3. 可以由程序安全补齐的机械内容由程序补齐，例如明确的 `y=...`/`z=...` 右侧表达式和符合现有几何关系的角度控制；
4. 复核模型只提供诊断建议；它不同意、超时或返回坏格式，都不能取消一份已经通过本地执行检查的课件；
5. 最终 OLL 的实际能力由程序从文档中读取并记录，而不是只回报规划模型给它贴的标签。

因此，开发者不应通过不断加提示词追求“模型永远按一种格式思考”。应该扩大本地可验证范围、把机械转换交给程序，并让无法验证的内容明确失败或局部降级。

当前仍有一个不能掩盖的限制：课程要求清单和本次生成能力范围仍由模型候选结果参与决定。来源映射、复核、最终检查和跨学科评测能发现很多漏项，但不能从逻辑上保证自然语言理解永不遗漏。后续改动不能把这个概率判断进一步变成隐藏的业务开关。

## 4. 仍未完成或仍需产品验收的部分

以下内容不能因为有原型或测试就写成已交付产品：

- DSL 和 npm 包仍是 `0.1` / `0.1.0-rc.1`，尚未承诺长期兼容；
- 复杂物理、化学、生物模拟插件尚未成为当前 OLL 的正式组件体系；
- 任意 3D 物体编辑、材质、碰撞、流体和通用仿真不在当前能力内；
- 课堂中途必须等待学生回答再继续的完整教学状态机尚未实现，当前任务在课程结束后开放；
- 真实触摸屏、触控笔、弱网、低性能设备、无障碍和长时间课堂仍需专项验收；
- 响应时间仍需继续优化。当前默认 `OLL_PARALLELISM=1`，以避免免费层或低配额 Vertex 并发 429；提高并发必须结合账号配额实测；
- 分支、PR 和发布状态会继续变化。本页只记录核对时刻，不能替代 `git` 和 GitHub 的实时状态。

## 5. 本次核对使用的证据

本次更新实际运行了：

- OLL `npm test`：178 项通过；
- learning-coach `npm test`：48 项通过，并确认 OLL 合同锁定 `991a247`；
- octos-web `pnpm test:unit`：935 项通过；
- 本书全部相对链接检查、JSON 代码块解析、代码围栏配对和 `git diff --check`。

本次没有重新运行 Octos 全部 Rust 测试，也没有重新发起付费/联网的 Vertex 真实生成评测。相关章节描述的是代码边界和已经存在的测试，不把这次文档更新冒充新的生产验收。

## 6. 如何维护这页

每次升级 OLL 时，至少更新：

1. 四个仓库的精确提交；
2. learning-coach 与 octos-web 锁定的 OLL commit；
3. 新增、删除或改变语义的 Runtime 能力；
4. 仍未完成的产品边界；
5. 对应自动测试和真实 E2E 证据。

不要写“目标集成版已经有”。要么给出代码和测试位置，要么明确标为后续方向。

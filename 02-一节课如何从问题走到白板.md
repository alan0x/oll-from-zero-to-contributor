# 02　一节课如何从问题走到白板

这一章先完整走一遍真实链路。暂时不纠结每个 JSON 字段。

## 1. 用户发出请求

用户在 `/learn` 底部输入：

> 请结合单位圆和 y=sin(x) 的函数图像，解释角度旋转如何变成周期波动。

octos-web 把这句话作为一次会话输入发给 Octos。若用户同时明确附加了白板选区，前端还会附带选区图片和稳定白板引用；没有附加时，不应偷偷把整块旧白板当成新问题来源。

## 2. Octos Agent 决定调用 Skill

Octos 把用户消息、系统指令、会话上下文和可用工具交给主模型。主模型判断这是一个需要白板课程的教学请求，于是调用：

```text
oll_generate_lesson
```

工具参数中最重要的是：

- `turn_id`：这一轮请求的稳定 ID；
- `learner_request`：用户完整问题；
- `request_source`：本题依据是什么；
- `language`：课程语言；
- 可选的用户背景、受控图片资源和白板引用。

`request_source` 有三种：

| 值 | 含义 |
| --- | --- |
| `self_contained` | 用户这句话本身就是完整问题 |
| `current_image` | 问题明确依赖当前摄像头或上传图片 |
| `explicit_board_follow_up` | 用户明确引用了当前白板中的既有内容 |

这里的原则是：系统必须知道本轮问题依据什么，不能拿模糊的历史上下文自行补题。

## 3. learning-coach 先整理课程要求

Skill 不会一上来就让模型输出整堂复杂课件。它先整理本轮必须满足的要求，例如：

- 教学目标：解释旋转角与周期波动的关系；
- 视觉对象：单位圆和正弦函数图；
- 视觉关系：把圆周点的纵坐标映射到正弦图；
- 共享变量：`theta`；
- 学生控制：滑杆和几何点；
- 动画：让 `theta` 从 `0` 变化到 `2π`；
- 教学任务：让学生把点移动到 `sin(theta)=1`。

这是一份 Skill 内部的课程要求清单，不是 OLL 标准，也不是用户需要学习的产品名。它的作用是防止生成模型漏掉用户明确要求。

接着，独立验证会检查：每条用户要求是否被表示为“要讲”“要画”“要交互”或“当前不支持”。不能因为规划模型遗漏，就让后面的生成阶段永远看不到这项要求。

## 4. Skill 选择本课需要的 OLL 能力

程序根据要求清单生成能力计划。例如本课需要：

```text
write: geometry, plot, note
actions: focus, connect, animate
lesson: variables, controls, tasks
bindings: geometry, plot
interaction: geometry angle control
```

这里不是写死“遇到单位圆就用某个模板”，而是把有限的教学需求映射到有限的 OLL 能力。

能力计划有两个用途：

1. 给生成模型一个较小、较明确的输出范围；
2. 生成后拒绝本课不允许的动作。

最终真相仍是完整 OLL Schema 和语义校验器。能力计划不能取代完整协议。

## 5. Skill 分段生成并动态交付

为了缩短首次可见内容的等待，Skill 可以把课程拆成若干相对独立的段落，并生成视觉组件：

1. 认识单位圆；
2. 认识正弦图；
3. 建立纵坐标映射；
4. 播放共享变量动画；
5. 开放学生任务。

当一个合法前缀完成时，Skill 会写出：

```text
<turn_id>.part-001.octos-lesson.json
```

并在 stderr 发送 Artifact 进度。全部完成后再写：

```text
<turn_id>.octos-lesson.json
```

部分 Artifact 不是五个独立课堂。它们是同一课堂不断增长的合法前缀，最终文件会替换同一轮的部分文件。

## 6. 每份课件都必须通过本地验收

模型返回 JSON 后，learning-coach 依次检查：

1. JSON 是否能解析；
2. 是否符合 Authoring Schema；
3. 引用、变量、内容和任务在语义上是否合法；
4. 是否覆盖课程要求；
5. 是否只使用能力计划允许的结构；
6. 是否能被 Normalizer 转为 Canonical；
7. Canonical 是否能被 Reducer 执行成白板状态。

必要时只修复失败的局部，而不是让模型无条件重写整堂课。

## 7. Skill 通过标准协议返回结果

learning-coach 是一个独立可执行程序。Octos 调用形式是：

```text
./main oll_generate_lesson
```

参数以 JSON 写入 stdin，最终结果以一个 JSON 对象写到 stdout：

```json
{
  "success": true,
  "output": "Validated OLL lesson generated.",
  "files_to_send": ["/absolute/path/to/turn.octos-lesson.json"]
}
```

日志和进度写 stderr，不能混进 stdout，否则 Octos 无法稳定解析最终结果。

## 8. Octos 交付文件和进度

Octos 收集 `files_to_send`，把文件纳入当前会话的工作区和文件投影，并通过 UI 协议通知前端。

前端可能依次看到：

- 工具开始；
- 课程要求整理完成；
- 第一部分课件到达；
- 后续部分到达；
- 最终课件到达；
- 工具完成。

右下角老师的等待动画属于 octos-web 产品反馈，不属于 OLL。

## 9. octos-web 下载并规范化课件

Artifact 保存的是 Authoring OLL JSON。前端下载后调用 OLL Core：

```ts
const events = normalizeAuthoringLesson(authoring, {
  lessonId,
  boardId,
  baseRevision,
  regionIntent,
  regionId,
});
```

得到 Canonical 事件后，前端才能交给 Player 和 Runtime。

对于后续追问，Authoring 可以携带明确的 `board_context`，Normalizer 会验证它是否与当前 `boardId` 和 revision 一致，并把局部别名解析到之前白板对象的稳定 ID。

## 10. Player 把事件编译成播放操作

一条 Canonical `lesson.step` 里包含多个 Beat。每个 Beat 又包含：

- `before_speech` 动作；
- narration 开始和结束；
- `during_speech` 动作；
- `after_speech` 动作；
- Beat 结束。

Player 把它们编译成线性的播放操作流，维护 cursor、播放状态、当前 Step、当前 Beat 和 checkpoint。

## 11. TTS 与动画如何配合

octos-web 获取下一段 narration，异步生成 TTS。准备期间，教师形象展示等待动画。TTS 开始时，前端通知 Player 当前 Beat 的旁白开始；TTS 完成时，再通知旁白结束。

因此 `during_speech` 的动画不会在 TTS 尚未到达时提前播完。OLL 只表达：

```json
{
  "do": "animate",
  "variable": "theta",
  "value": 6.283185307179586,
  "duration_intent": "extended"
}
```

实际持续多少毫秒由 Runtime 根据旁白、播放设置和降低动态效果偏好决定。

## 12. Web Runtime 渲染并接收操作

Web Runtime 从 Semantic Board State 读取节点、连接、分组、焦点和变量，然后：

- 计算相对布局；
- 渲染文字、公式、Geometry、Plot、Scene3D 等节点；
- 对 Binding 求值；
- 显示滑杆；
- 接收圆周点拖动；
- 接收 3D 旋转和缩放；
- 判断学生任务进度；
- 保存操作日志和播放 checkpoint。

当 `theta` 变化时，只有一份变量状态。滑杆、自动动画和圆周拖点都修改它，Geometry 和 Plot 都从它重新求值。

## 13. 刷新后如何恢复

恢复并不是只保存一张白板截图。系统需要组合：

- 会话中的 OLL Artifact；
- Canonical 事件；
- Player checkpoint；
- 学生变量操作；
- 3D 视角操作；
- Ink SVG 文档；
- 选区来源和局部增强记录。

它们拥有不同的生命周期，所以分别保存。恢复时重新执行确定性协议，而不是把旧 DOM 原样塞回页面。

## 14. 到这里应该形成的认识

一堂课不是“模型返回 JSON，前端画出来”这么简单。它是一条有明确责任边界的管线：

```text
用户要求
→ Agent 决策
→ Skill 规划与生成
→ Authoring OLL
→ 完整校验
→ Artifact 交付
→ Canonical Normalization
→ Playback Operations
→ Semantic Board State
→ DOM/SVG/3D 与学生交互
```

后面的章节会逐层拆开这条管线。

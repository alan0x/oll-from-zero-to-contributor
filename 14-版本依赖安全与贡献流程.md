# 14　版本、依赖、安全与贡献流程

## 1. 四仓库依赖图

```text
octos-lesson-language
├── learning-coach（生成与校验 Authoring）
└── octos-web（normalize/player/runtime/ink）

octos
├── 安装并运行 learning-coach
└── 为 octos-web 提供 API/UI 协议
```

因此 OLL 变化最容易向下游扩散。

## 2. 不要依赖“我本地正好有这个分支”

每个消费方必须在 `package.json` 和 lockfile 中锁定明确的 OLL commit 或发布版本。文档、TODO 和 PR 应写清：

- 仓库；
- 分支；
- commit；
- PR；
- 上游依赖 commit；
- 测试状态；
- 是否可合并。

临时 worktree 只是开发工具，不是交付物。删除 worktree 前必须确认提交已存在于正常仓库引用或远端分支。

## 3. 协议版本和包版本

当前包版本是 RC，DSL 版本为 `0.1`。二者含义不同：

- npm package version：发布物版本；
- `dsl/version/profile`：课件协议身份；
- commit hash：开发期间精确依赖；
- Schema SHA-256：learning-coach vendored 合同是否一致。

修改内部实现但不改协议，可以只升包版本或 commit；破坏协议兼容时必须有明确迁移和版本决定。

## 4. Artifact 兼容

会话里可能长期保存旧 Artifact。升级 Runtime 时要回答：

- 旧 Authoring `0.1` 还能否 normalize？
- 旧 checkpoint 是否拒绝不匹配程序？
- 旧 Ink 格式是否有迁移？
- 旧 selection snapshot 版本能否验证？
- 学生操作日志是否向后兼容？

不能只测试新生成的新文件。

## 5. 安全边界

### 模型输出

- 必须 JSON parse 和 Schema 校验；
- 必须语义校验；
- 数学表达式不得 `eval`；
- 文字渲染不得注入任意 HTML；
- 图片只能使用宿主授权 asset；
- Provider 原始错误需截断和脱敏。

### Skill

- manifest env allowlist；
- stdout 只输出协议；
- 文件路径必须在工作目录内；
- `files_to_send` 必须是明确文件；
- 超时、取消和并发受控。
- 直接 Skill Action 先协商 `skill.actions.v1`，再验证 action ID 和输入；
- 选区媒体只能从 `OCTOS_SESSION_WORKSPACE` 内读取，解析真实路径后仍须防越界。

### 白板引用

- 只允许用户明确附加的 reference；
- board ID/revision 必须匹配；
- target ID 来自 Runtime；
- 不把整板静默发送给模型；
- 过期引用失败关闭。

### 学生笔迹

- 原始 SVG 不因识别而修改；
- 选区来源有 checksum；
- 识别结果带置信度；
- 辅助内容与原稿分开；
- 删除/隐藏增强不删除原稿。

## 6. 隐私

摄像头、上传图片和白板笔迹可能包含敏感信息。最小化原则：

- 只上传完成本轮任务所需区域；
- 局部提问只发送局部选区；
- 日志不记录原始图片；
- Artifact 不包含凭据；
- 用户能知道并控制何时附加白板上下文；
- 不把临时视觉识别永久写成用户事实。

## 7. 性能预算

完整课程与局部增强应分别设预算：

### 完整课程

- 要求整理时间；
- 首段可用时间；
- 全部完成时间；
- 重试率；
- 首轮 OLL 合法率；
- 首屏 TTS 等待。

### 局部增强

- 选区截图时间；
- 识别时间；
- 模型响应时间；
- 本地渲染时间；
- 首次返回成功率。

不能因为完整课程从 129 秒降到 41 秒，就默认局部解释也可以等 41 秒。

## 8. Pull Request 的合理拆分

跨仓库能力通常至少有：

1. OLL 协议/Runtime PR；
2. learning-coach 依赖和生成 PR；
3. octos-web 依赖和产品集成 PR；
4. 仅在必要时的 Octos PR。

每个 PR 都应独立说明自己的职责，不把多个仓库的事实混成一句“已经完成”。

## 9. PR 描述应包含

- 用户问题和目标行为；
- 设计边界；
- 修改文件/模块；
- 协议是否改变；
- 依赖的上游 commit/PR；
- 自动测试；
- 人工/E2E 步骤；
- 已知限制；
- 回滚方式；
- 截图或录屏（视觉变化时）。

## 10. Code Review 重点

### OLL Review

- 是否真需语言扩展；
- 是否破坏确定性；
- 是否有语义校验；
- 引用和生命周期是否清楚；
- 是否有负向 fixture；
- Runtime 与 Core 最终状态是否一致。

### learning-coach Review

- 用户要求是否可能静默漏掉；
- 本次生成能力映射是否按通用 OLL 能力工作，而不是按题目写分支；
- Provider Schema 是否过大或不兼容；
- 失败是否局部修复；
- 是否泄露模型/凭据数据；
- 性能指标是否真实。
- 是否把复核模型或规划模型的概率性结论当成业务开关；
- 最终报告的能力是否来自实际 Lesson，而不是规划标签；
- 本地机械转换是否按 OLL 结构泛化，而不是按测试题关键词枚举。

### Octos Review

- Provider 适配是否只作用于副本；
- 是否影响所有工具；
- Skill 进程协议是否稳定；
- 文件和进度是否有稳定关联 ID；
- 超时、取消和错误类型是否正确。

### octos-web Review

- 是否复用 OLL Runtime；
- Artifact 是否正确去重和恢复；
- 世界坐标是否统一；
- 无障碍、触摸和 reduced motion；
- 弱网/TTS 等待反馈；
- 原稿不变原则。
- 学生操作是否错误暂停 TTS；
- 重播结束后旧笔迹和新笔迹是否都恢复；
- focus 是否保留正在讲解的主视觉并避开宿主浮层。

## 11. 提交前的最小证据

至少给出：

- 相关仓库 `git status`；
- 精确 diff 范围；
- 格式、类型、单测结果；
- 固定 fixture 结果；
- 若涉及生成，真实 eval 结果；
- 若涉及产品，真实 `/learn` E2E；
- 依赖 commit 和 lockfile diff。

## 12. 一个新贡献者的第一项任务

建议从以下任务开始：

- 修一个 OLL 文档和示例不一致；
- 给现有语义校验添加一个负向测试；
- 给 Web Runtime 补一个 fragment focus 测试；
- 给 learning-coach 的一个 violation 增加更清楚的错误；
- 给 octos-web Artifact 排序补边界用例。

先完成一条小而完整的证据链，再尝试新增内容类型或跨仓库协议。

## 13. 最终心智模型

贡献者不应把项目看作“大模型生成一个炫酷白板”。更准确的模型是：

```text
自然语言教学需求
→ 受约束的课程创作
→ 可验证的语义协议
→ 确定性的事件和状态
→ 可恢复的播放与交互
→ 清楚区分老师内容、学生原稿和系统辅助内容
```

每一层都能单独测试、失败和演进。这就是 OLL 让整个产品可贡献、可维护的原因。

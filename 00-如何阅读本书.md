# 00　如何阅读本书

## 1. 你不需要预先知道什么

本书假定读者会阅读基本的 JSON 和 TypeScript，但不假定你了解教育产品、Agent、DSL、事件流、Reducer 或无限白板。

先记住三个最普通的事实：

1. 用户提出一个学习问题。
2. 系统生成一份结构化课件。
3. 浏览器照着课件说话、写字、画图并接收学生操作。

OLL 就处在第 2 步和第 3 步之间。

## 2. 本书里的三个标记

### 已实现

指目标集成版本中存在代码、协议和测试的能力。例如共享变量、动画、滑杆、几何控制点、学生任务、3D 和学生笔迹保存。

### 设计原则

指开发时必须维持的边界。例如“学生原稿不可被系统偷偷修改”和“前端不直接执行 Authoring OLL”。它不一定对应一个单独字段，但应由多处代码共同保证。

### 可扩展方向

指合理但尚未成为当前协议的方向。例如复杂物理仿真插件。它会明确标成未来方向，不能据此编写依赖当前 Runtime 的生产课件。

## 3. 学习 OLL 时最容易犯的错误

### 错误一：先背完整 Schema

Schema 是机器验证用的完整合同，不适合当作第一份教材。先理解 Lesson、Step、Beat、Action，再按需查看内容类型。

### 错误二：把 OLL 当成绘图 API

OLL 说“把正弦图放在单位圆右边”，不说“创建一个宽 420 像素的 SVG，坐标为 `(620, 180)`”。布局坐标是 Runtime 的工作。

### 错误三：把所有状态都塞进 OLL

老师生成的课件、学生笔迹、学生操作日志、播放进度和浏览器相机位置是不同的数据。它们可以共同恢复课堂，但不应混成一份万能 JSON。

### 错误四：只看最终白板

最终白板相同，不代表课程相同。先写公式再解释，和一次性铺满整页，是完全不同的教学过程。OLL 的价值之一就是记录过程。

### 错误五：在错误的仓库修问题

例如：

- 模型不会生成 `plot`：先查 learning-coach 的规划、生成和校验；
- 合法 `plot` 画错：查 OLL Web Runtime；
- 文件没有到达前端：查 Octos 的 Skill 结果和 UI 协议；
- 文件到了但 `/learn` 没装载：查 octos-web；
- Schema 允许了语义上无效的引用：查 OLL Core。

## 4. 建议的动手方式

每读一章都做一个小动作：

1. 在 OLL 示例目录找到一份 Authoring JSON。
2. 找到它对应的 Canonical JSONL。
3. 在独立 Harness 中播放。
4. 在 Core 中找到负责校验或转换的函数。
5. 在 octos-web 中找到消费该数据的组件。

这样你建立的是“字段—代码—界面”的联系，而不是只记名词。

## 5. 需要的基础工具

- Node.js 20 或更高版本；
- npm；
- pnpm；
- Rust 工具链；
- Git；
- 一个能运行现代浏览器的本地环境。

四个仓库使用的包管理器并不相同：

| 仓库 | 主要技术 | 包管理/构建 |
| --- | --- | --- |
| OLL | TypeScript | npm |
| learning-coach | TypeScript/Node.js | npm |
| Octos | Rust | cargo |
| octos-web | React/TypeScript | pnpm |

不要在一个仓库里照搬另一个仓库的命令。

## 6. 如何使用本书定位问题

如果你看到的是语言结构问题，读第 3—6 章。

如果你看到的是播放、恢复、动画或交互问题，读第 7 章。

如果你看到的是生成慢、生成失败、内容缺失，读第 8 章。

如果你看到的是 Skill 调用、模型 Provider、文件传输或事件问题，读第 9 章。

如果你看到的是 `/learn` UI、TTS、白板选择和会话恢复问题，读第 10—11 章。

如果你准备写代码，读第 12—14 章，并使用附录 C 的检查清单。

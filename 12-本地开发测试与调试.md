# 12　本地开发、测试与调试

## 1. 不要一开始就启动四个系统

最有效的顺序是从最小边界向外扩展：

1. OLL Core 单元测试；
2. OLL 独立 Harness；
3. learning-coach mock Provider 测试；
4. learning-coach 真实模型评测；
5. Octos Skill 安装和调用；
6. octos-web `/learn` E2E。

每层通过后再进入下一层，才能知道错误首次出现在哪里。

## 2. OLL 仓库

```bash
cd /Users/alan0x/Documents/projects/octos-lesson-language
npm install
npm run typecheck
npm test
npm run check:examples
```

启动 Harness：

```bash
npm run harness:dev
```

访问：

```text
http://127.0.0.1:4173
```

修改 Core、Player、Web Runtime 或 Ink 后，先在这里验证。

## 3. learning-coach 仓库

```bash
cd /Users/alan0x/Documents/projects/learning-coach
npm install
npm run oll:check
npm run build
npm test
npm run contract:vertex
```

运行真实模型评测前确认：

- 凭据通过受管环境注入；
- 模型名、Project 和 Location 正确；
- 重试次数受控；
- 不会把凭据打印到日志；
- 知道该命令会联网并产生费用。

固定视觉用例：

```bash
npm run eval:visual -- --case unit-circle-to-sine
```

## 4. Octos 仓库

先读仓库根目录 `AGENTS.md`，它包含 workspace 结构、测试和提交要求。

典型检查：

```bash
cd /Users/alan0x/Documents/projects/octos
cargo fmt --all -- --check
cargo check -p octos-llm
cargo test -p octos-llm
cargo test -p octos-agent
cargo test -p octos-cli --features api --lib
```

涉及 Gemini Tool Schema 时，必须有 Provider adapter 的单元测试，证明：

- 不支持关键字被移除或转换；
- 支持字段保留；
- 原始 ToolSpec 没有被修改；
- 不同 Provider 不互相污染。

## 5. 安装 learning-coach

先在 Skill 源目录构建：

```bash
cd /Users/alan0x/Documents/projects/learning-coach
npm run build
```

再安装到实际用于 `/learn` 的 Profile：

```bash
octos skills --profile <profile-id> install /Users/alan0x/Documents/projects/learning-coach --force
```

然后确认：

```bash
octos skills --profile <profile-id> info learning-coach
```

只重新编译 Octos 不会自动更新已安装 Skill；只重新构建 Skill 也不会自动替换已安装副本。要明确自己改的是哪一层。

## 6. octos-web 仓库

```bash
cd /Users/alan0x/Documents/projects/octos-web
pnpm install
pnpm run lint
pnpm run test:unit
pnpm run build
pnpm dev
```

浏览器 E2E：

```bash
pnpm test
```

真实语音/摄像头测试需要 HTTPS、本机权限和音频解锁；不要用纯单元测试结果代替真实设备验收。

## 7. 固定课件测试和真实生成测试

两者回答不同问题：

### 固定 fixture

使用仓库内已知正确 Canonical 课程。验证：

- Player；
- Runtime；
- 视觉布局；
- TTS 调度；
- 学生控制；
- 持久化。

它不验证模型会不会生成。

### 真实生成

用户输入自然问题，由 Agent 和 Skill 生成课程。验证：

- 工具选择；
- 请求来源；
- 要求整理；
- 模型生成质量；
- Provider Schema；
- Artifact 交付；
- 完整产品链路。

如果固定 fixture 错，不要先改 learning-coach Prompt；如果 fixture 对但真实生成错，再查生成链。

## 8. 推荐 E2E 用例

### 数学共享变量

> 请结合单位圆和 y=sin(x) 的函数图像，解释角度旋转如何变成周期波动。

观察：Geometry、Plot、映射、同一变量、动画、滑杆、拖点、刷新恢复。

### 物理运动

> 为什么弹簧会往复运动？请结合物体的位置变化图像，解释它为什么可以用余弦函数描述。

观察：真实用户语言能否被规划为物理示意、Plot、共享变量和动画，而不是依赖测试术语。

### 3D

> 请用一个可以旋转的立方体解释正视图、俯视图和侧视图的区别。

观察：Scene3D、预设视角、旋转、复位和视角任务。

### 局部增强

1. 在白板写 `y=2x+1`；
2. 框选；
3. 点“生成函数图像”；
4. 确认原稿不变，图像出现在旁边；
5. 刷新后确认二者恢复。

## 9. 日志按阶段记录

learning-coach 应记录：

- turn ID；
- stage；
- started/completed/failed；
- elapsed_ms；
- 模型名；
- attempt；
- Schema fingerprint/大小；
- 输入和输出 token；
- violation code；
- 发布了第几个 part。

不要在普通日志中记录凭据、完整 Authorization、用户整张图片或未经脱敏的模型原始内容。

## 10. 400 错误怎么分层

### 错误路径是 `tools[...].function_declarations`

主 Agent 的工具参数 Schema 被 Provider 拒绝。查 Octos `octos-llm` Provider adapter。

### 错误发生在 `lesson-authoring` 或 `selection-enhancement`

Skill 内部结构化生成 Schema 被 Vertex 拒绝。查 learning-coach 的 Provider response schema。

### Provider 接受但 OLL validation 失败

查模型输出、能力计划、完整 Schema 和语义校验 violation。

三种问题看起来都可能是 HTTP 400 或“白板生成失败”，但根因完全不同。

## 11. 性能调试

至少拆成：

```text
agent tool selection
requirements planning
requirements verification
first section generation
visual component generation
local validation/assembly
first artifact transport
frontend download/normalize
first narration TTS
first visible board action
final artifact
```

“总共 41 秒”只能说明慢，不能说明哪里慢。

## 12. 页面刷新测试

刷新后依次确认：

- 会话文件重新列出；
- Artifact 按 turn/rank 去重；
- Canonical 重新组成同一课堂；
- checkpoint 匹配；
- 变量最终值恢复；
- 3D 视角恢复；
- Ink SVG 恢复；
- selection source 和 enhancement 关系恢复；
- 不重复播放已完成 TTS；
- 不重复应用同一个 action。

## 13. 不要用人工 E2E 代替自动测试

每次人工发现的问题，都应下沉为最小自动回归：

- Schema/语义问题 → OLL Core test；
- Player 状态问题 → Player test；
- 视觉/交互问题 → Web Runtime browser test；
- 生成合同问题 → learning-coach mock test；
- Provider 兼容问题 → adapter contract test；
- 产品编排问题 → octos-web unit/Playwright。

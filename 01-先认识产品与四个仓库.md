# 01　先认识产品与四个仓库

## 1. 用户看到的不是四个系统

用户只看到一个学习产品：

- 在屏幕底部输入问题或说话；
- 老师开始讲解；
- 白板逐步出现文字、公式、图形和图像；
- 用户拖动滑杆、图上的点或 3D 视角；
- 用户可以在白板上自由书写；
- 用户可以框选一部分内容，请系统解释、检查或生成函数图；
- 刷新后，课程、操作和笔迹仍能恢复。

为了让这个体验可维护，工程上必须拆分职责。

## 2. 为什么需要 OLL

最直接的做法，是让模型输出一段自然语言，再让前端猜哪里是标题、哪里是公式、哪里该画图。这会产生三个问题：

1. 前端无法可靠理解自然语言里的隐含结构；
2. 课程无法逐 Beat 播放、暂停、重放和恢复；
3. 模型输出稍有变化，前端就可能失效。

另一个极端，是让模型直接输出 Canvas 或 SVG 命令。这样又会产生：

1. 模型必须处理像素、坐标和浏览器实现细节；
2. 同一课件无法在不同屏幕和 Runtime 上复用；
3. 很难验证“引用是否存在”“动画变量是否已声明”“学生任务是否有控制入口”。

OLL 取中间路线：它描述教学语义和相对关系，把机械执行交给程序。

## 3. OLL 仓库负责什么

OLL 是 Octos Lesson Language 的缩写。它是一种领域专用语言，也就是只为“AI 教师课程”这个领域设计的结构化语言。

OLL 仓库拥有：

- Authoring Profile 的 JSON Schema；
- Canonical Profile 的 JSON Schema；
- TypeScript 类型；
- 结构校验和语义校验；
- Authoring 到 Canonical 的确定性 Normalizer；
- Canonical 到白板状态的 Reducer；
- 无 DOM 的 Player；
- 浏览器 Web Runtime；
- 学生笔迹 Ink Runtime；
- 示例、非法样本、模型评测和教学播放门禁。

它不负责：

- 调用 Gemini 等模型生成课程；
- 决定当前 Agent 是否该生成一堂课；
- 管理用户身份、聊天会话或 HTTP API；
- 实现 `/learn` 的完整产品界面。

## 4. learning-coach Skill 负责什么

`learning-coach` 是一个可以被 Octos 调用的 Skill。这里的 Skill 不是提示词文件，而是带 `manifest.json` 和可执行程序的工具包。

它提供两个主要工具：

- `oll_generate_lesson`：根据用户请求生成一整堂或继续当前白板的 OLL 课程；
- `oll_enhance_selection`：根据用户明确框选的内容生成局部解释、建议或函数图增强。

这里要区分“工具实现”和“用户入口”。`oll_enhance_selection` 仍是 Skill 内部工具，但 `/learn` 的“解释这部分”等按钮通过 manifest 中的 `learning.selection.enhance` Skill Action 直接调用它，不再先把按钮操作交给主 Agent 猜测。完整课程仍由主 Agent 选择 `oll_generate_lesson`。

它负责：

- 整理本轮用户要求；
- 选择本课需要的 OLL 表现能力；
- 调用模型生成 Authoring OLL；
- 校验用户要求是否被覆盖；
- 使用锁定的 OLL Schema 和语义校验器验收结果；
- 保存课件文件；
- 把部分课件和最终课件作为 Artifact 交给 Octos。

课程规划模型给出的结果只是候选教学设计。learning-coach 还要用本地代码检查最终 OLL 实际包含的节点、Binding、控件和任务，不能把模型自己写的“能力标签”当成可执行证明。

它不负责在浏览器中画图，也不应该重新实现 OLL Runtime。

## 5. Octos 后端负责什么

Octos 是通用 Agent Runtime 和服务端。

它负责：

- 接收用户输入；
- 运行 Agent 循环；
- 向模型暴露可用工具；
- 根据模型返回的 tool call 执行 Skill；
- 对不同模型 Provider 做协议适配；
- 管理 Skill 的安装、环境变量、超时和并发；
- 管理会话、工作目录和文件；
- 通过 UI 协议把消息、工具进度和文件交付给前端。
- 协商并执行前端发起的 Skill Action；
- 把会话工作区安全地提供给需要读取选区图片的 Skill。

Octos 不解释 `write geometry` 是什么意思。对它来说，OLL 课件只是 Skill 生成并声明要交付的文件。课堂语义由 OLL 和前端 Runtime 负责。

## 6. octos-web 负责什么

`octos-web` 是 React 前端，`/learn` 是其中的学习工作区。

它负责：

- 输入框、语音、摄像头和教师形象；
- 接收 Octos 的会话事件、工具进度和文件；
- 下载 Skill 生成的 OLL Artifact；
- 调用 OLL Normalizer，把 Authoring 课件转为 Canonical 事件；
- 把多次生成的课程追加到同一课堂；
- 使用 OLL Player 和 Web Runtime 播放；
- 生成和衔接 TTS；
- 提供课程目录、播放控制、无限白板和持久化；
- 挂载 Ink Runtime；
- 把用户选区变成显式引用或局部增强请求。

octos-web 不应该自己定义另一套 OLL 语义。若一种合法 OLL 内容在独立 Harness 和 `/learn` 中表现不同，优先检查二者是否真的使用同一个 Runtime 和同一版本。

## 7. 四个仓库如何协作

```text
用户
  │ 提问
  ▼
octos-web /learn
  │ UI 协议消息
  ▼
Octos Agent
  │ 调用工具
  ▼
learning-coach Skill
  │ 生成 Authoring OLL 文件
  ▼
Octos 会话文件与事件
  │ 交付文件
  ▼
octos-web
  │ OLL Normalizer + Player + Web Runtime
  ▼
用户看到并操作白板课程
```

OLL 仓库像一条横向基础设施，同时被 learning-coach 和 octos-web 依赖：

```text
                   octos-lesson-language
                    ▲               ▲
                    │               │
          learning-coach         octos-web
                    ▲               ▲
                    └──── Octos ────┘
```

## 8. 一个重要边界：谁生成机械字段

模型负责：

- 课程标题、目标和讲解；
- Step、Beat 和动作；
- 哪些对象需要建立联系；
- 哪个变量驱动哪些图；
- 学生应该完成什么任务。

程序负责：

- 全局稳定 ID；
- sequence；
- revision；
- 幂等键；
- 像素坐标；
- 动画毫秒和帧率；
- 存储路径；
- 权限验证。

如果模型能少写而程序能确定性补齐，就应让程序补齐。这是 OLL 可生成性的关键。

## 9. 判断职责是否放错了的三个问题

遇到一个新字段或新功能时，依次问：

1. 它是否表达教学含义？如果是，可能属于 OLL Authoring。
2. 它是否只是稳定执行所需的机械信息？如果是，可能属于 Canonical 或宿主。
3. 它是否只与某个界面的视觉实现有关？如果是，通常属于 Runtime 或产品 UI，而不是语言。

例如“动画持续时间意图为 extended”是教学意图，属于 OLL；“动画恰好 6400ms”是设备和旁白条件下的执行决定，属于 Runtime。

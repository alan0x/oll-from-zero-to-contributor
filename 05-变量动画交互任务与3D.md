# 05　变量、动画、交互任务与 3D

## 1. 为什么需要共享变量

单位圆上的点和正弦图上的点表示同一个角度。如果两张图各存一份角度：

- 动画可能只更新其中一张；
- 滑杆可能更新 A 状态；
- 几何拖点可能更新 B 状态；
- 刷新后恢复的又是第三份状态。

OLL 的做法是在 Lesson 顶层声明一次变量，所有视图都绑定它。

## 2. 声明变量

```json
{
  "lesson": {
    "mode": "explain",
    "language": "zh-CN",
    "title": "从单位圆到正弦波",
    "goals": ["理解旋转和周期波动的关系"],
    "variables": [
      {
        "as": "theta",
        "initial": 0,
        "min": 0,
        "max": 6.283185307179586,
        "label": "旋转角 θ",
        "unit": "rad",
        "control": {
          "kind": "slider",
          "step": 0.01
        }
      }
    ]
  }
}
```

变量是 Lesson 全局的。允许声明多个变量，只要别名唯一，并满足数量和范围限制。

当前没有 Beat 内局部变量。原因很实际：变量需要跨 Beat 驱动节点、动画、学生操作、任务判断和刷新恢复。Beat 局部生命周期会让这些语义变得模糊。

`unit` 只用于显示，不会自动进行单位换算。

## 3. Binding：把节点字段绑定到变量表达式

Geometry 中的圆周点：

```json
{
  "as": "point-p",
  "x": 1,
  "y": 0,
  "interaction": {
    "kind": "angle_control",
    "variable": "theta",
    "center": "origin"
  }
}
```

同一 Geometry 内容中声明 Binding：

```json
{
  "target": "point-p.x",
  "expression": "cos(theta)"
},
{
  "target": "point-p.y",
  "expression": "sin(theta)"
}
```

Plot 中的移动点：

```json
{
  "target": "wave-point.x",
  "expression": "theta"
},
{
  "target": "wave-point.y",
  "expression": "sin(theta)"
}
```

当 `theta` 变化时，Reducer/Runtime 对表达式重新求值，两张图同步更新。

## 4. 表达式不是 JavaScript

表达式只允许：

- 数字；
- 已声明变量；
- `pi`、`e`；
- 白名单算术运算；
- 白名单数学函数，如 `sin`、`cos`。

它不能访问 DOM、网络、全局对象，也不能调用任意代码。表达式由 OLL 自己的安全解析器处理，不应使用 `eval`。

Binding 不能引用另一个 Binding 的结果。每条表达式必须直接由变量计算，避免求值顺序依赖。

## 5. 多个变量如何使用

例如抛射运动可声明：

```json
"variables": [
  {"as":"time","initial":0,"min":0,"max":5,"unit":"s"},
  {"as":"speed","initial":10,"min":1,"max":20,"unit":"m/s","control":{"kind":"slider","step":1}}
]
```

某条 Binding 可以同时引用 `time` 和 `speed`：

```text
speed * time
```

多个变量是允许的，但每增加一个变量，学生理解成本和任务状态空间都会增大。不要因为技术上允许就把所有常量都改成滑杆。

## 6. 自动动画

```json
{
  "do": "animate",
  "variable": "theta",
  "value": 6.283185307179586,
  "easing": "linear",
  "duration_intent": "extended",
  "when": "during_speech"
}
```

模型只指定目标值、缓动意图和时长意图。Player 维护动画语义，Web Runtime 负责中间帧。

Reducer 不模拟每一帧，而是确定性地把变量推进到终值。这样 Headless 测试不会依赖墙上时钟。

## 7. 同一变量的四个入口

在产品里，`theta` 可能由四种入口修改：

1. OLL `animate`；
2. 学生拖动 slider；
3. 学生拖动圆周控制点；
4. 学生点击 reset。

它们必须进入同一变量状态通道。Runtime 不能为每个 UI 控件维护私有角度。

一次连续拖动应记录为一条学生操作：

```text
start → update → update → commit
```

只有 commit 的最终值进入持久化日志，避免每一帧都写一条业务记录。

## 8. 学生任务

变量目标任务示例：

```json
{
  "as": "reach-sine-maximum",
  "prompt": "拖动圆周点，让 sin θ 等于 1。",
  "availability": {"kind":"after_lesson"},
  "allowed_operations": [
    {
      "kind": "variable_change",
      "variable": "theta",
      "controls": ["slider", "geometry_point"]
    }
  ],
  "completion": {
    "kind": "expression_target",
    "expression": "sin(theta)",
    "value": 1,
    "tolerance": 0.02
  },
  "hints": [
    "观察圆周点的纵坐标。",
    "最高点的纵坐标是多少？"
  ],
  "hint_after_attempts": 2,
  "success_message": "完成了：圆周最高点对应正弦最大值。"
}
```

任务合同同时说明：

- 学生看到什么要求；
- 哪些操作被允许；
- Runtime 如何确定完成；
- 失败几次后显示提示；
- 成功后显示什么。

模型不负责在运行时主观判断学生有没有完成。可验证条件交给确定性 Runtime。

## 9. 为什么任务通常在课后开放

当前 `availability.kind` 是 `after_lesson`。这样课程播放和学生控制不会在同一时刻争夺变量。

“讲到一半停下来等待学生回答”的教学控制需要更完整的课堂状态机，不应假装已经由 `mode: explain` 支持。

## 10. 3D Scene

`scene3d` 用于描述可旋转、缩放和恢复视角的三维教学对象，例如：

- 立方体及其顶点和边；
- 坐标轴和三维曲面；
- 平面截面；
- 预设相机视角。

OLL 表达三维语义和初始相机，不直接输出 WebGL 指令。Web Runtime 负责实际渲染。

典型 3D 任务：

```json
{
  "as": "find-top-view",
  "prompt": "旋转立方体，找到从正上方观察的视角。",
  "availability": {"kind":"after_lesson"},
  "allowed_operations": [
    {
      "kind": "scene3d_view",
      "node": "cube-scene",
      "controls": ["orbit", "zoom", "preset", "reset"]
    }
  ],
  "completion": {
    "kind": "scene3d_view_target",
    "node": "cube-scene",
    "match": "view_direction",
    "yaw": 0,
    "pitch": -1.5708,
    "zoom": 1,
    "angular_tolerance": 0.12,
    "zoom_tolerance": 0.2
  },
  "hints": ["让视线方向垂直指向立方体顶面。"]
}
```

`view_direction` 只比较观察方向；`camera_pose` 还会考虑完整相机姿态。二者不能混为同一个任务。

## 11. 变量和 3D 不是“任意模拟器”

当前 OLL 能表达受控数学变量、二维几何/函数联动、基础 3D 视角和截面。复杂化学反应、刚体碰撞、流体、电路求解等，不应把计算规则全部塞进通用 DSL。

未来合理方向是经过验证的教学组件：OLL 传参数、教学目标、输入和输出，版本化组件负责专业计算。但这是扩展方向，不是当前课件可以假定存在的能力。

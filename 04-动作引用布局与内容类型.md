# 04　动作、引用、布局与内容类型

## 1. `write` 的基本形状

每个 `write` 都回答五个问题：

```json
{
  "do": "write",
  "as": "sine-plot",
  "kind": "plot",
  "role": "explanation",
  "content": {},
  "place": {}
}
```

- 做什么：`write`；
- 创建后叫什么：`sine-plot`；
- 是什么内容类型：`plot`；
- 在教学中扮演什么角色：`explanation`；
- 内容和相对位置是什么。

## 2. 内容类型

当前 `write kind` 包含：

| kind | 典型用途 |
| --- | --- |
| `text` | 普通文字、句子、段落和可寻址文本片段 |
| `math` | LaTeX 公式及公式片段 |
| `shape` | 简单视觉形状 |
| `diagram` | 流程、关系和示意图 |
| `geometry` | 可度量的二维几何对象 |
| `plot` | 坐标轴、函数曲线、点和辅助线 |
| `scene3d` | 三维对象、相机和截面 |
| `image` | 宿主授权的图片和图片区域 |
| `table` | 行列结构数据 |
| `note` | 标题加简短正文的教学卡片 |

`diagram` 和 `geometry` 不等价。若需要准确的圆、坐标、角度、投影和拖动控制点，使用 `geometry`；若只需要表达“细胞膜控制物质进出”这样的概念关系，使用 `diagram`。

## 3. Fragment：节点内部可被引用的部分

Node 不是最小引用单位。许多内容内部可以声明 Fragment：

- text/math 的 `fragments`；
- plot 的 `curves`、`points`、`guides`；
- geometry 的 `points`、`circles`、`arcs` 等可寻址对象；
- diagram 的 `elements`、`edges`、`regions`；
- image 的授权 `regions`。

引用格式：

```text
node-alias#fragment-alias
```

例如：

```text
sine-plot#moving-point
unit-circle#point-p
formula#sin-term
```

这使老师可以强调曲线上的特定点，而不是只能强调整张图。

## 4. `connect` 表达跨对象关系

```json
{
  "do": "connect",
  "as": "projection-mapping",
  "from": "unit-circle#point-p",
  "to": "sine-plot#moving-point",
  "relation": "maps_to",
  "label": "相同的纵坐标"
}
```

Connection 是 Semantic Board State 中的一等对象，因此后续可以：

```json
{
  "do": "emphasize",
  "target": "projection-mapping",
  "emphasis": "focus"
}
```

不要为了版面好看而滥用连接。`relation` 应能说明教学含义，例如 `maps_to`、`determines`、`compares_with`、`supports`。

## 5. `revise` 是显式修改

已有对象不能通过重复同一个 `as` 来覆盖。必须使用：

```json
{
  "do": "revise",
  "target": "definition-note",
  "content": {
    "title": "补充后的定义",
    "text": "sin θ 是单位圆上点 P 的纵坐标"
  },
  "reason": "在观察图形后补充坐标含义"
}
```

显式修订让回放、审计和恢复知道对象发生过什么变化。

学生笔迹不适用这套 revise。系统对学生原稿的原则是只读，在旁边生成新的辅助内容。

## 6. `group` 组织知识结构

```json
{
  "do": "group",
  "as": "core-mapping",
  "role": "explanation",
  "label": "从圆到波",
  "members": ["unit-circle", "sine-plot"]
}
```

Group 可以作为布局 anchor 和 focus 目标。它不是 DOM 容器；Runtime 决定视觉上如何表现。

## 7. `focus`、`point` 和 `emphasize` 的区别

- `focus`：决定当前镜头和注意区域；
- `point`：老师做一次指向动作；
- `emphasize`：给内容留下或切换视觉强调状态。

同一 Beat 可能三者并用：先聚焦两张图，再指向映射点，最后强调纵坐标关系。

## 8. 相对布局

Authoring 不写坐标，而写 `place`：

```json
{
  "relation": "right_of",
  "anchor": "unit-circle",
  "align": "start",
  "gap": "normal"
}
```

关系包括：

```text
new_region
below
above
left_of
right_of
near
inside
overlay
```

除了 `new_region`，一般都需要 `anchor`。`anchor` 必须引用已存在的 Node 或 Group。

布局是约束，不是最终位置。Web Runtime 会结合节点实际尺寸、屏幕宽度、碰撞和白板现状计算位置。

## 9. `new_region` 的意义

```json
{
  "relation": "new_region",
  "region_role": "lesson_origin"
}
```

它告诉 Runtime 这是一个新的内容起点。宿主还会提供 `regionIntent`：

- `new_topic`
- `continue_topic`
- `extend_near_anchor`

Authoring 表达局部相对关系，宿主决定这轮 Lesson 如何放入长期课堂。

## 10. 图片不是任意 URL

模型不能随意输出外部图片 URL。图片来自宿主提供的 `ResourceContext`：

```json
{
  "asset_id": "asset-geometry-001",
  "regions": [
    {
      "as": "point-a",
      "source_region": "asset-geometry-001#region-a",
      "label": "A",
      "confidence": "high"
    }
  ]
}
```

后续只引用本地别名：

```text
source-image#point-a
```

这样资源权限、内容来源和视觉定位由宿主控制，模型不能编造看不见的区域。

## 11. Board Context：明确引用旧白板

当用户明确说“请继续解释我刚才圈出的这个点”，前端可以传入稳定引用：

```json
{
  "board_context": {
    "board_id": "learning-board-session-1",
    "revision": 12,
    "references": [
      {
        "as": "previous-plot",
        "type": "node",
        "target_id": "node-stable-id",
        "label": "上一轮正弦图",
        "fragments": [
          {
            "as": "selected-point",
            "target_id": "node-stable-id#point-1"
          }
        ]
      }
    ]
  }
}
```

新 Lesson 可以使用 `previous-plot#selected-point`。Normalizer 会把别名解析为已有稳定 ID。

这不是让模型读整块白板。它只获得用户明确附加、宿主验证过的引用。

## 12. 引用错误为什么必须尽早失败

以下情况都应在 Core 中失败：

- 引用尚未创建的别名；
- 把 Connection 当布局 anchor；
- 引用不存在的 Fragment；
- `board_context` revision 与宿主不一致；
- 图片区域不在 ResourceContext；
- 新对象和旧对象的类型不符合动作要求。

如果错误直到浏览器渲染才暴露，说明协议校验边界太弱。

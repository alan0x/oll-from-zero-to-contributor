# 11　整堂课与白板局部增强

产品有两个相邻但不同的创作入口。区分它们能同时改善体验、速度和工程边界。

## 1. 模式一：Composer 生成一整堂课

入口是屏幕底部 composer。用户通常说：

> 为什么弹簧会往复运动？请结合图像解释它和余弦函数的关系。

系统目标：

- 理解完整教学问题；
- 规划教学目标和结构；
- 生成多个 Step/Beat；
- 生成旁白和多种视觉内容；
- 建立共享变量、动画和任务；
- 交付一堂可播放、可复习的课。

这条路径允许数十秒生成，但必须动态反馈并尽快交付第一段。

## 2. 模式二：白板选区局部增强

用户在已有白板上圈选内容，工具栏出现有限选项。用户点击“解释这部分”。

系统目标：

- 只理解选中的局部；
- 保留原稿；
- 生成一块紧邻的辅助内容；
- 不重新规划整堂课；
- 在数秒级返回。

这不是缩短版完整 Lesson，而是独立的局部增强协议和 Artifact。

## 3. 为什么不能只靠“意图识别自动执行”

用户写下 `y=x²`，可能想：

- 画函数图；
- 检查书写；
- 解释函数性质；
- 围绕它讲一课；
- 什么也不做，只是记笔记。

系统可以做轻量识别来排序候选工具，但不能仅凭识别就自动改白板。正确 UX 是：框选后显示少量可能操作，由用户选择。

## 4. 两种模式的共同入口

选区工具“围绕这部分讲一课”把局部模式桥接到 Composer 模式：

1. 创建不可变选区快照；
2. 找到重叠的语义白板对象；
3. 将选区作为 composer reference；
4. 用户补充问题；
5. 发送 `explicit_board_follow_up`；
6. learning-coach 生成完整后续 OLL Lesson；
7. 新 Lesson 在当前白板附近继续。

用户明确选择了上下文，系统没有擅自读取整板。

## 5. “我圈了一个图，然后问这是什么”

目标集成版本可以这样处理：

### 若用户使用选区工具

1. Ink Runtime 生成 lasso 快照；
2. octos-web 截取选区局部图片；
3. 查找圈选区域覆盖的 OLL Node/Fragment；
4. 视觉识别模型只观察局部图片；
5. 用户点“解释这部分”或输入自定义问题；
6. 调用 `oll_enhance_selection`；
7. 在旁边显示解释。

### 若用户在 composer 直接问“这是什么”

只有当 composer 中附带了明确选区 reference，系统才把它作为 `explicit_board_follow_up`。没有引用时应该请用户框选或说清对象，而不是自行猜测。

## 6. 语义目标和视觉识别互补

只传截图的问题：模型能看形状，但不知道它是哪个稳定节点，也无法可靠延续引用。

只传语义 ID 的问题：知道这是 `plot#moving-point`，但看不到学生在上面画的圈或箭头。

因此局部上下文同时包含：

- 选区局部图片；
- 原始 Ink SVG 快照；
- OLL Semantic Board Targets；
- 世界坐标关系；
- 识别置信度；
- 用户选择的工具或问题。

## 7. 不修改原稿的合同

所有局部工具都满足：

```text
source ink: immutable
enhancement: new content
relationship: derived_from / explains / suggests
```

即使用户写错字或函数写错，系统也不能在原位置静默改掉。它可以在旁边标注：

> 这里可能想写的是 `sin`，请确认。

并明确置信度和建议性质。

## 8. 局部函数图如何生成

用户圈选 `y=2x+1` 并点“生成函数图像”：

1. 视觉模型识别公式；
2. 识别结果必须是明确的单变量 `y=f(x)`；
3. learning-coach 输出 Plot Enhancement；
4. 安全表达式解析器验证只含 `x` 和白名单函数；
5. Web Runtime/前端 Plot 渲染器采样；
6. 图像放在选区旁边；
7. 原公式不变。

识别为低置信度时，应先让用户确认公式，而不是画一张可能错误的图。

## 9. 性能目标不同

完整课程需要规划、验证、多段生成和整体校验。局部增强只需要：

- 一次局部视觉识别；
- 一次小型结构化响应；
- 小型本地校验；
- 局部渲染。

因此两者不应共用同一个 40 秒以上管线。局部工具应有独立小 Schema、较低 token 上限、缓存和更严格的响应时限。

## 10. 可以缓存什么

在 source checksum 未变化时可缓存：

- 选区图片；
- 视觉识别文本和置信度；
- 内容类型；
- 稳定 Board Targets；
- 同一 tool_id 的结果。

用户换了问题、文档版本或选择范围后，缓存 key 必须变化。

## 11. 失败体验

局部增强失败不应破坏课程或原稿：

- 保留选区；
- 显示简短可重试提示；
- 可切换为自定义问题；
- 不向整堂课插入半成品节点；
- 日志记录失败阶段和 source ID，不记录秘密或整张白板。

## 12. 产品上如何区分两种结果

完整课程：

- 进入课程目录；
- 有 Step/Beat 播放；
- 可生成 TTS；
- 可包含任务和动画；
- 作为会话 Lesson Artifact 保存。

局部增强：

- 紧邻来源显示；
- 不进入长课程目录；
- 默认不自动长篇朗读；
- 可隐藏或再次提问；
- 作为 Selection Enhancement Artifact 保存。

这两个模式共享白板和上下文引用，但不应共享所有生成和播放开销。
